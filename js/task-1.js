'use strict';

const name = '«Генератор защитного поля»';
const price = 1000;

const sameMessage = `Выбран ${name}, цена за штуку ${price} кредитов.`;

console.log(sameMessage);

const newPrice = 2000;

const sameNewMessage = `Выбран ${name}, цена за штуку ${newPrice} кредитов.`;

console.log(sameNewMessage);
